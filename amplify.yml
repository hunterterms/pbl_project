version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p public
        - cp *.html public/index.html
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
